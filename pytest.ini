[pytest]
addopts = -ra
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests that require external services or long-running setups
    e2e: marks end-to-end tests
    network: marks tests that perform real network calls
testpaths =
    tests
asyncio_mode = auto
